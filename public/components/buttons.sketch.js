var primary_button = { x: 10, y: 10, w: 100, text: 'Hello!' }
var secondary_button = {}

function setup() {
  createCanvas(400,400);
}

function draw (){
    background(255);
    button (primary_button);
}